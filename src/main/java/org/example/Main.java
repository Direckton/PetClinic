package org.example;

import org.example.Controller.Controller;

public class Main {
    /**
     * @param args Arguments are collected from user based on submenu, the program doesn't take any initial arguments in this version
     */
    public static void main(String[] args) {
        Controller controller = new Controller();
    }
}